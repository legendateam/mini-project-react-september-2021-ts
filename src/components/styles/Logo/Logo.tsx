import React from 'react';

const logo = require('./logo__header_img.webp');

const Logo = () => {
    return (
        <>
            <img src={logo} alt={logo}/>
        </>
    );
};

export default Logo;