export interface IPropsSelect {
    PaperProps: {
        style: {
            maxHeight: number,
            width: number,
        },
    }
}

export interface IMenuProps {
    MenuProps: IPropsSelect
}